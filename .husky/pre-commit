if [ -f 'bun.lock' ]; then
	bun lint-staged
elif [ -f 'package-lock.json' ]; then
	npm run lint-staged
elif [ -f 'yarn.lock' ]; then
	yarn lint-staged
elif [ -f 'pnpm-lock.yaml' ]; then
	pnpm lint-staged
else
	echo "No lock file found."
	exit 1
fi